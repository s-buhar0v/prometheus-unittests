rule_files:
  - rules.yml
evaluation_interval: 1m
tests:
  - interval: 1m
    input_series:
      - series: "up"
        values: "10x1"
    alert_rule_test:
      - alertname: simpleTest
        eval_time: 1m
        exp_alerts:
          - exp_labels:
              severity: page
            exp_annotations:
              description: "hello world"
